//PRIORITY 1
//WORKS:
//Scale
//Trans
//Rename
start->(){box}
box->scale(5,10,5){box2}
//box2->trans(1,5,1){trans}
box2->comp(){bottom|top|side|side|side|side}
side->subDiv(Y,2,1r,2){two|six|two}
two->trans(10,0,0){two}

END
//After END no line will be evaluated!

Stuff Todo: 
SUBDIV translations
RENAME epsilon -> .inactive does not work
REPEAT infinit loop ??
SUBDIV does not take parameters (absolute, relative, absolute)... does it only take 2 parameters?? 